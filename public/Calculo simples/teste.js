

function somar(props) {
    window.onload = () => {
        alert('oi')
    }
    //C = parseInt(props.B) + parseInt(props.A)
    return props.A + " + " + props.B +" = "
    
}

export default somar